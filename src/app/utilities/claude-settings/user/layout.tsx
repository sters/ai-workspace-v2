import type { Metadata } from "next";

export const metadata: Metadata = { title: "Claude Settings: User" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
