import type { Metadata } from "next";

export const metadata: Metadata = { title: "Claude Settings: Local" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
